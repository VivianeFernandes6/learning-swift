let label = "The width is "
let width = 94
let widthLabel = label + String(width) // para printar como frase, eu posso converter um tipo de constante em outra. 
// para converter de inteiro para string, posso só fazer o seguinte: String(constante_int)

print(widthLabel)